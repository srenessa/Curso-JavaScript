function randomFraction() {

    // JavaScript tem uma função Math.random() que gera um número decimal aleatório entre 0 (inclusive) e 1 (exclusivo).
    let value = 0;
    while (value === 0) {

        return Math.random();
    }

}