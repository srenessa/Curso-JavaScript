// Compreendendo a distinção entre maiúsculas e minúsculas em variáveis

// MYVAR não é o mesmo que MyVar nem myvar. É possível ter múltiplas variáveis ​​distintas com o mesmo nome, mas com maiúsculas e minúsculas diferentes

// Escreva nomes de variáveis ​​em JavaScript em camelCase. No camelCase, nomes de variáveis ​​com várias palavras têm a primeira palavra em minúscula e a primeira letra de cada palavra subsequente é maiúscula.

// Modifique as declarações e atribuições existentes para que seus nomes usem camelCase. 

// Variable declarations
var studlyCapVar;
var properCamelCase;
var titleCaseOver;

// Variable assignments
studlyCapVar = 10;
properCamelCase = "A String";
titleCaseOver = 9000;