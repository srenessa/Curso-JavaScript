// você usará essa sintaxe se apenas um valor estiver sendo exportado de um arquivo.
// não pode usar o padrão de exportação com var, let ou const
export default function subtract(x, y) {
    return x - y;
}