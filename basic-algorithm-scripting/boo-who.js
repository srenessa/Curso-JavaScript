// Nessa função, utilizamos o operador typeof para verificar se o tipo de value é igual a 'boolean'. Se for igual, a função retorna true, caso contrário, retorna false.

function booWho(bool) {

    return typeof bool === "boolean";
}

booWho(null);